spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:task_tracker_test
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        dialect: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create
